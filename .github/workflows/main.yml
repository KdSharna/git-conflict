name: Todo Pipeline

on:
  push:
    paths:
      - 'todo_infra/**'
  pull_request:
    paths:
      - 'todo_infra/**'

jobs:
  run-todo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: List files in todo_infra
        run: |
          echo "Listing todo_infra folder:"
          ls -la todo_infra/
